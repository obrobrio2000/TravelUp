# Questo script chiude tutti i container, ricostruisce ed avvia. Utile sia per l'avvio che per il riavvio (ad es. dopo una modifica).
# Utilizzo: ./start
# Per avviare in modalità detached (= tiene il terminale libero), utilizzare: ./startd
docker-compose down --remove-orphans
docker-compose up --build --remove-orphans
